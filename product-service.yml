spring:
  application:
    name: product-service

  data:
    mongodb:
      uri: mongodb://localhost:27017/productdb

server:
  port: 8081

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
